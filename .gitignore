### Шаблон Windows
# Файлы кэша миниатюр Windows
Thumbs.db
Thumbs.db: зашифрованный
ehthumbs.db
ehthumbs_vista.db

# Файл дампа
*.stackdump

# Файл конфигурации папки
[Dd]esktop.ini

# Корзина, используемая в общих файловых ресурсах
$RECYCLE.BIN/

# Установочные файлы Windows
*.cab
*.msi
*.msix
*.msm
*.msp

# Ярлыки Windows
*.lnk

### Шаблон для macOS
# Общие сведения
.DS_Store
.AppleDouble
.LSOverride

Значок # должен заканчиваться двумя \r
Значок

# Эскизы
._*

# Файлы, которые могут отображаться в корневом каталоге тома
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.не предлагать

# Каталоги, потенциально созданные на удаленном общем ресурсе AFP
.AppleDB
.AppleDesktop
Сетевая  папка "Корзина"
Временные элементы
.apdisk

### Шаблон Linux
*~

# временные файлы, которые могут быть созданы, если процесс все еще использует дескриптор удаленного файла
.fuse_hidden*

# Настройки каталога KDE
.directory

# Папка корзины Linux, которая может появиться на любом разделе или диске
.Trash-*

# .nfs создаются, когда открытый файл удаляется, но к нему по-прежнему осуществляется доступ
.nfs*

### Шаблон JetBrains
# Поддерживает IDE JetBrains: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio и WebStorm
# Ссылка: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

*.iml
*.ipr
*.iws
/.idea/*

# Исключить материалы, не относящиеся к конкретному пользователю
.идея!/.name
.идея!/codeInsightSettings.xml
.идея!/codeStyles/
.идея!/авторские права/
.идея!/dataSources.xml
.идея!/detekt.xml
.идея!/encodings.xml
.идея!/externalDependencies.xml
.идея!/file.template.settings.xml
.идея!/fileTemplates/
.идея!/icon.svg
.идея!/inspectionProfiles/
.идея!/runConfigurations/
.идея!/области/
.идея!/vcs.xml

### Шаблон Kotlin
# Скомпилированный файл класса
*.class

# Файл журнала
*.log

# Файлы пакетов #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# журналы сбоев виртуальной машины, см. http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*

### Шаблон Gradle
.gradle

# Обратите внимание, что вам может потребоваться вручную исключить другие папки
# именованная сборка, если необходимо (например, в src/)
**/build/

# Избегайте игнорирования файла jar-оболочки Gradle (файлы.jar обычно игнорируются)
gradle!/оболочка/gradle-wrapper.jar

# Кэш проекта
.gradletasknamecache